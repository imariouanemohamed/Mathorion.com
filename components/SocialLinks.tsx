import { socials,SocialKey } from "@/data/socials"; import { FaYoutube,FaTiktok,FaInstagram,FaFacebookF,FaLinkedinIn,FaXTwitter,FaGithub,FaPinterestP,FaWhatsapp } from "react-icons/fa6";
const icons:Record<SocialKey,React.ComponentType>={youtube:FaYoutube,tiktok:FaTiktok,instagram:FaInstagram,facebook:FaFacebookF,linkedin:FaLinkedinIn,x:FaXTwitter,github:FaGithub,pinterest:FaPinterestP,whatsapp:FaWhatsapp};
export function SocialLinks({compact=false}:{compact?:boolean}){return <div className={compact?"social-icons":"social-grid"}>{socials.map(s=>{const Icon=icons[s.icon];const content=<><span className="social-icon"><Icon/></span>{!compact&&<span><strong>{s.name}</strong><small>{s.handle}</small></span>}</>;return s.url?<a key={s.name} href={s.url} target="_blank" rel="noreferrer" aria-label={s.name}>{content}</a>:<span key={s.name} className="social-pending" title="Link coming soon" aria-label={`${s.name} link coming soon`}>{content}</span>})}</div>}
